#!/usr/bin/env bash
set -euo pipefail

here="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PATH="${here}/usr/bin:${PATH}"
export EMEDITOR_WINE_DESKTOP_EXEC="${APPIMAGE:-${here}/AppRun}"
exec "${here}/usr/bin/emeditor-wine" "$@"
